-------------------------------------------
-- Event1380


local states ={}

states.a = {
  name = "潮信",
  desc = "又到了潮流更替的时候了",
  choices = {"好的"}
  }
function states.a:choose(choice)
    if choice == self.choices[1] then
      --调用潮信的buff系统
      return nil
    end
end

EventExample = Event(states.a)
-------------------------------------
