-------------------------------------------
-- EventExample


local states ={}

states.a = {
  name = "女孩的请求1",
  desc = "提供10点食物",
  choices = {"好的","我没有"},
  }
function states.a:choose(choice)
    if choice == self.choices[1] then
      --if GameData..food>10 then
      --调用给奖励的菜单接口 奖励池子待定
      return nil
    elseif choice==self.choices[2] then
      --调用弹对话的接口 text= "你个辣鸡！"
      return nil
    end
end

Event1011 = Event(states.a)
Event1011.states = {}
-------------------------------------
