-------------------------------------------
-- Event1383


local states ={}

states.a = {
  name = "起雾",
  desc = "起雾了，人民的心情好像不太好",
  choices = {"好的"}
  }
function states.a:choose(choice)
    if choice == self.choices[1] then
      --调用起雾的buff系统
      return nil
    end
end

EventExample = Event(states.a)
-------------------------------------
