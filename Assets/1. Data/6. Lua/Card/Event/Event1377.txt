-------------------------------------------
-- Event1377


local states ={}

states.a = {
  name = "台风",
  desc = "近日将有台风登陆",
  choices = {"好的"}
  }
function states.a:choose(choice)
    if choice == self.choices[1] then
      --调用台风的buff系统
      return nil
    end
end

EventExample = Event(states.a)
-------------------------------------
