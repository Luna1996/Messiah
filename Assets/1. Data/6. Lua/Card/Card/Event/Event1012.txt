-------------------------------------------
-- Event1012


local states ={}

states.a = {
  name = "米西亚的拒绝",
  desc = "米西亚拒绝为城市提供矿石线索",
  choices = {"为什么！！"}
  }
function states.a:choose(choice)
    if choice == self.choices[1] then
      --调用米西亚的拒绝buff
      return nil
    end
end

Event1012 = Event(states.a)

-------------------------------------
